{
  "name": "salesforce-app",
  "private": true,
  "version": "1.0.0",
  "description": "Salesforce App",
  "keyword": [
    "salesforce",
    "apex",
    "test",
    "mock"
  ],
  "scripts": {
    "lint": "npm run lint:apex ; npm run lint:doc",
    "lint:apex": "sfdx scanner:run -v -t 'force-app/classes/src/*.cls' -f table -c 'apex-mockery'",
    "lint:doc": "write-good README.md --parse",
    "postinstall": "husky install ; ./postInstall.sh",
    "prettier": "prettier --write \"**/*.{cls,cmp,component,css,html,js,json,md,page,trigger,xml,yaml,yml}\"",
    "prettier:verify": "prettier --list-different \"**/*.{cls,cmp,component,css,html,js,json,md,page,trigger,xml,yaml,yml}\"",
    "precommit": "lint-staged",
    "prepush": "npm run test:coverage && npm run lint",
    "test": "sfdx force:apex:test:run -r human -d ./tests/apex -s ApexMockery -w 20",
    "test:coverage": "npm run test -- -c"
  },
  "devDependencies": {
    "@commitlint/cli": "^17.3.0",
    "@commitlint/config-angular": "^17.3.0",
    "@prettier/plugin-xml": "^2.2.0",
    "husky": "^8.0.2",
    "lint-staged": "^13.0.4",
    "prettier": "^2.8.0",
    "prettier-plugin-apex": "^1.11.0",
    "write-good": "^1.0.8"
  },
  "lint-staged": {
    "**/*.{cls,cmp,component,css,html,js,json,md,page,trigger,xml,yaml,yml}": [
      "prettier --write"
    ],
    "README.md": [
      "write-good --parse"
    ]
  }
}
